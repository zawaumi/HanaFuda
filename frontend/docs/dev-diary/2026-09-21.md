# 2026-09-21

## 12:05 — Issue #14 ホーム画面実装

- 担当: Codex
- ブランチ: `feature/frontend-home`
- 目的: 会う直前に、ホームから最短で会話準備を始められるようにする
- 実施:
  - 初対面の相手登録への導線を追加
  - 登録なしで使える「さくっと話題」を3件追加
  - モックの会話履歴から最近話した人を抽出して表示
  - 相手ごとの会話設定予定ルートへの導線を追加
  - 読み込み、空、エラー、再試行の状態を追加
  - PCとモバイルに対応するホーム専用スタイルを追加
- 判断:
  - API・モック基盤Issue #13へ依存するため、そのブランチを基点にした
  - 実API接続時にビルド中の通信を避けるため、ホームデータはブラウザで取得する
  - 相手登録フォームとデッキ生成は後続Issueの予定ルートへつなぐだけに留める
- 変更箇所:
  - `frontend/src/app/page.tsx`
  - `frontend/src/features/home/`
- 確認:
  - `npm run check` — 成功、警告なし
  - `npm run build` — 成功
- 次:
  - Issue #13のマージ後、ホーム画面PRのbaseを `develop` にする

## 12:20 — Issue #15 つながり一覧・検索実装

- 担当: Codex
- ブランチ: `feature/frontend-connections`
- 目的: 登録済みの相手を検索し、詳細確認や次の会話準備へ進めるようにする
- 実施:
  - 名前、関係、メモを対象にしたキーワード検索を追加
  - 相手一覧と検索件数を表示
  - 相手詳細、会話設定、新規登録の予定ルートへの導線を追加
  - 読み込み、空、検索結果なし、エラー、再試行の状態を追加
  - PCとモバイルに対応する一覧専用スタイルを追加
- 判断:
  - 検索はデータ取得境界の `getPersons` を使い、実API切り替え後も画面コードを維持する
  - 入力中の過剰な通信を避けるため、検索を250ms遅延させる
  - Issue #14の次に積むことで、同日の開発日記を競合なく追記する
- 変更箇所:
  - `frontend/src/app/connections/page.tsx`
  - `frontend/src/features/connections/`
- 確認:
  - `npm run check` — 成功、警告なし
  - `npm run build` — 成功
- 次:
  - 依存PRのマージ後、このPRのbaseを `develop` に変更する
