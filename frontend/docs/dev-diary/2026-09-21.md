# 2026-09-21

## 12:05 — Issue #14 ホーム画面実装

- 担当: Codex
- ブランチ: `feature/frontend-home`
- 目的: 会う直前に、ホームから最短で会話準備を始められるようにする
- 実施:
  - 初対面の相手登録への導線を追加
  - 登録なしで使える「さくっと話題」を3件追加
  - モックの会話履歴から最近話した人を抽出して表示
  - 相手ごとの会話設定予定ルートへの導線を追加
  - 読み込み、空、エラー、再試行の状態を追加
  - PCとモバイルに対応するホーム専用スタイルを追加
- 判断:
  - API・モック基盤Issue #13へ依存するため、そのブランチを基点にした
  - 実API接続時にビルド中の通信を避けるため、ホームデータはブラウザで取得する
  - 相手登録フォームとデッキ生成は後続Issueの予定ルートへつなぐだけに留める
- 変更箇所:
  - `frontend/src/app/page.tsx`
  - `frontend/src/features/home/`
- 確認:
  - `npm run check` — 成功、警告なし
  - `npm run build` — 成功
- 次:
  - Issue #13のマージ後、ホーム画面PRのbaseを `develop` にする

## 12:20 — Issue #15 つながり一覧・検索実装

- 担当: Codex
- ブランチ: `feature/frontend-connections`
- 目的: 登録済みの相手を検索し、詳細確認や次の会話準備へ進めるようにする
- 実施:
  - 名前、関係、メモを対象にしたキーワード検索を追加
  - 相手一覧と検索件数を表示
  - 相手詳細、会話設定、新規登録の予定ルートへの導線を追加
  - 読み込み、空、検索結果なし、エラー、再試行の状態を追加
  - PCとモバイルに対応する一覧専用スタイルを追加
- 判断:
  - 検索はデータ取得境界の `getPersons` を使い、実API切り替え後も画面コードを維持する
  - 入力中の過剰な通信を避けるため、検索を250ms遅延させる
  - Issue #14の次に積むことで、同日の開発日記を競合なく追記する
- 変更箇所:
  - `frontend/src/app/connections/page.tsx`
  - `frontend/src/features/connections/`
- 確認:
  - `npm run check` — 成功、警告なし
  - `npm run build` — 成功
- 次:
  - 依存PRのマージ後、このPRのbaseを `develop` に変更する

## 12:48 — Issue #16 相手登録画面実装

- 担当: Codex
- ブランチ: `feature/frontend-person-create`
- 目的: 初対面の相手を最低限の入力で登録し、会話設定へ進めるようにする
- 実施:
  - 名前、関係、知っていること、会う状況の入力フォームを追加
  - 名前が空欄の場合に「名前不明」で登録する処理を追加
  - 必須入力と文字数のクライアント検証を追加
  - 項目付近のエラー、送信失敗、送信中、登録成功の表示を追加
  - 送信中の入力とボタンを無効化し、二重送信を防止
  - 登録した相手と会う状況を会話設定予定ルートへ引き継ぐ導線を追加
- 判断:
  - SNS・URL入力やバックエンド検証は対象外とした
  - 登録成功を確認できるよう、即時遷移せず成功画面から会話設定へ進む構成にした
  - Issue #15の後続ブランチとして実装し、画面導線と日記を競合なく積み上げる
- 変更箇所:
  - `frontend/src/app/connections/new/page.tsx`
  - `frontend/src/features/connections/person-create-form.tsx`
  - `frontend/src/features/connections/person-create-form.module.css`
- 確認:
  - `npm run check` — 成功、警告なし
  - `npm run build` — 成功
- 次:
  - 会話設定画面Issueで引き継いだ状況を初期値として利用する

## 12:59 — Issue #17 相手詳細画面実装

- 担当: Codex
- ブランチ: `feature/frontend-person-detail`
- 目的: 相手との関係、記憶、直近の会話を確認して次の会話準備へ進めるようにする
- 実施:
  - URLのperson_idから基本プロフィールを取得して表示
  - 知っていること、関係タグ、確認済み記憶、直近の会話を表示
  - 次の会話設定予定ルートへの導線を追加
  - 読み込み、404、通信エラー、再試行、情報不足時の表示を追加
  - 記憶一覧取得をデータ取得境界とAPI契約へ追加
  - PCとモバイルに対応する詳細画面スタイルを追加
- 判断:
  - Personにタグ項目がないため、現行API契約にあるrelationshipを関係タグとして表示する
  - 記憶は画面からモックを直接参照せず、`getPersonMemories`をmock/remote共通境界へ追加する
  - Issue #16の後続ブランチとして実装し、同日の開発日記を継続する
- 変更箇所:
  - `frontend/src/app/connections/[id]/page.tsx`
  - `frontend/src/features/connections/person-detail.tsx`
  - `frontend/src/features/connections/person-detail.module.css`
  - `frontend/src/lib/api/`
  - `frontend/docs/api-contract.md`
- 確認:
  - `npm run check` — 成功、警告なし
  - `npm run build` — 成功
- 次:
  - FastAPI実装時に記憶一覧GETのレスポンス形式をOpenAPIと照合する
