# 2026-09-20

## TalkDeck MVP仕様のフロントエンド方針確認

- 担当: Codex
- ブランチ: `docs/talkdeck-spec-v0.2`
- 目的: 既存仕様書とモックから、フロントエンドが実装する画面とAPI境界を明確にする
- 実施:
  - MVPの画面、ユーザーフロー、デッキ表示、API契約を整理
  - モックで追加された検索、さくっと話題、相手詳細、再生成の導線を確認
- 判断:
  - バックエンドはFastAPIを使用する
  - フロントエンドとバックエンドの結合点をHTTP APIとJSONに限定する
  - フロントエンド担当は `backend/` を変更しない
- 変更箇所:
  - フロントエンド実装方針の前提となる共有仕様
- 確認:
  - 画面一覧とAPI入出力の整合を確認
- 次:
  - 実装時にFastAPIのOpenAPIとフロントエンド型を照合する

## フロントエンド初期環境

- 担当: Codex
- ブランチ: `feature/frontend-setup`
- 目的: バックエンドから独立したフロントエンド開発環境を用意する
- 実施:
  - Next.js 16、React 19、TypeScript、Tailwind CSS 4、ESLintを導入
  - 環境変数の雛形とnpmスクリプトを追加
  - 構成、開発手順、FastAPI連携方針を `frontend/docs/` に整理
- 判断:
  - FastAPIの既定URLを `http://localhost:8000` とする
  - APIベースURLは `NEXT_PUBLIC_API_BASE_URL` から取得する
  - 秘密情報とLLM APIキーはフロントエンドに置かない
- 変更箇所:
  - `frontend/`
- 確認:
  - `npm run check` — 成功
  - `npm run build` — 成功
  - npm audit — 脆弱性0件
- 次:
  - モックをもとに共通レイアウトとホーム画面を実装する

## 15:31 — 開発日記の適用範囲をfrontendへ限定

- 担当: Codex
- ブランチ: `feature/frontend-setup`
- 目的: AI駆動開発の日記を残しつつ、他担当の作業ルールへ影響させない
- 実施:
  - 日記を `frontend/docs/dev-diary/` に配置
  - 日記更新ルールを既存の `frontend/AGENTS.md` に追加
  - フロントエンドのドキュメント一覧から日記へリンク
  - 先にルートへ追加した日記とルールを削除
- 判断:
  - 日記とAI向けルールは `frontend/` 配下だけに適用する
  - バックエンド担当には同じ運用を強制しない
- 変更箇所:
  - `frontend/AGENTS.md`
  - `frontend/docs/README.md`
  - `frontend/docs/dev-diary/README.md`
  - `frontend/docs/dev-diary/2026-09-20.md`
- 確認:
  - ルートに `AGENTS.md` と記録ファイルが残っていないことを確認
- 次:
  - 以降のフロントエンド作業で日記を追記する
