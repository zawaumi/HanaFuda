# MVP横断QA（2026-09-22）

## 今回確認できたこと

- `npm run check`、`npm run build` は成功。
- 開発サーバーの `/`、`/connections`、`/connections/new`、`/history`、`/profile`、`/deck/generating`、`/deck/session`、`/deck/session/feedback` はHTTP 200。
- 画面コード上で、ナビゲーションの現在地・スキップリンク、フォームラベル、必須項目のエラー、非同期画面のloading・空・エラー、再試行導線を点検。プロフィール読込エラーの再試行を追加。
- 360px付近のCSSを点検。サイドバーはモバイルナビへ切替、カードとフォーム操作は縦方向に並ぶ指定。デッキ詳細はネイティブの `details` / `summary` を使用。

## 実機で残る確認

この環境ではブラウザ操作インターフェースが利用できず、画面幅変更・キーボード操作・アクセシビリティ自動検査は実行できていません。以下をブラウザで確認してください。実APIの通し確認は #24 の環境準備も必要です。

| 操作 | OKの状態 |
|---|---|
| 360pxとPC幅でホーム→相手登録→会話設定→生成→会話記録→履歴を進む | 横スクロール・重なり・切れがなく、最後の記録が履歴に現れる |
| Tab/Shift+Tab、Enter/Spaceのみで同じ操作を進める | フォーカスが見え、スキップリンク、ラジオ、記憶の確認、`details` の開閉を操作できる |
| 空データ・通信停止・404・422・5xxを各画面で再現する | 理由と次の操作が見え、画面が操作不能にならない |
| axe等でホーム、登録、設定、デッキ、記録、履歴を検査する | 重大なアクセシビリティ違反が0件 |

## スコープ

このPRはフロントのみ。`backend/` は変更しません。実機・実API確認が終わるまではDraftのままとします。
