# 2026-09-20

## TalkDeck MVP仕様書 v0.2

- 担当: Codex
- ブランチ: `docs/talkdeck-spec-v0.2`
- 目的: 既存仕様書とモックを統合し、3人チームが実装判断に使える仕様を作る
- 実施:
  - MVP範囲、画面、ユーザーフロー、データモデル、API、完了条件を整理
  - モックで追加された検索、さくっと話題、相手詳細、再生成の導線を反映
  - バックエンドをFastAPIとし、`frontend/` と `backend/` を完全分離する方針を明記
- 判断:
  - フロントエンドとバックエンドの結合点をHTTP APIとJSONに限定
  - フロントエンド担当は `backend/` を変更しない
- 変更箇所:
  - `docs/talkdeck_spec_v0.2.md`
- 確認:
  - Markdownの構成と差分を確認
  - `develop` 向けPR #2を作成
- 次:
  - 実装開始前の未決事項をチームで合意する

## フロントエンド初期環境

- 担当: Codex
- ブランチ: `feature/frontend-setup`
- 目的: バックエンドから独立したフロントエンド開発環境を用意する
- 実施:
  - Next.js 16、React 19、TypeScript、Tailwind CSS 4、ESLintを導入
  - 環境変数の雛形とnpmスクリプトを追加
  - 構成、開発手順、FastAPI連携方針を `frontend/docs/` に整理
- 判断:
  - FastAPIの既定URLを `http://localhost:8000` とする
  - APIベースURLは `NEXT_PUBLIC_API_BASE_URL` から取得する
  - 秘密情報とLLM APIキーはフロントエンドに置かない
- 変更箇所:
  - `frontend/`
- 確認:
  - `npm run check` — 成功
  - `npm run build` — 成功
  - npm audit — 脆弱性0件
- 次:
  - モックをもとに共通レイアウトとホーム画面を実装する

## AI駆動開発日記の導入

- 担当: Codex
- ブランチ: `feature/frontend-setup`
- 目的: 今後の全作業について、内容・判断・検証結果をチームで追跡可能にする
- 実施:
  - リポジトリ共通の作業ルールを `AGENTS.md` に追加
  - 日記の保存先、記入形式、禁止事項を定義
  - 仕様策定とフロントエンド環境構築の記録を初回エントリーとして追加
- 判断:
  - 日記は `docs/dev-diary/YYYY-MM-DD.md` に日本時間で追記する
  - 作業と日記を原則同じブランチ・コミットに含める
  - 秘密情報やチャット全文は記録しない
- 変更箇所:
  - `AGENTS.md`
  - `docs/dev-diary/README.md`
  - `docs/dev-diary/2026-09-20.md`
- 確認:
  - 必須項目と既存の担当境界が記載されていることを確認
- 次:
  - 以降のすべての作業で日記を追記する
